# Server-side only (never exposed to the browser)
API_URL=http://localhost:3001
# API_KEY=your-api-key-here

# Public: only used for WebSocket connection (no secret)
# NEXT_PUBLIC_WS_URL=ws://localhost:3001

# Optional: Dashboard WebSocket auth token
# NEXT_PUBLIC_DASHBOARD_TOKEN=some-secret-token
